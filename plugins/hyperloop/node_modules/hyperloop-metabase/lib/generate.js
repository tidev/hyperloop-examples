/**
 * Hyperloop Metabase Generator
 * Copyright (c) 2015 by Appcelerator, Inc.
 */
var gen = require('./generate/index');

// for testing
if (module.id === ".") {
	var fs = require('fs');
	!fs.existsSync('build') && fs.mkdirSync('build');
	!fs.existsSync('build/hyperloop') && fs.mkdirSync('build/hyperloop');
	return gen.generate('build/hyperloop', 'build/metabase-9.0-iphonesimulator-8d8d67e2682c5613536037287ce5a7d1.json', function (err) {
		if (err) {
			console.error(err);
			process.exit(1);
		}
	});
}
